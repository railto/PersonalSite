{
    "scripts": {
        "dokku": {
            "predeploy": "php artisan migrate --no-interaction --force"
        }
    },
    "cron": [
        {
            "command": "php artisan schedule:run >> /dev/null 2>&1",
            "schedule": "* * * * *"
        }
    ]
}
